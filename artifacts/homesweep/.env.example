# ── Required for production (set this in Vercel dashboard) ──
# The full URL of your Railway API server, NO trailing slash
VITE_API_URL=https://your-api-name.up.railway.app

# ── Local development only ────────────────────────────────
# Leave VITE_API_URL unset locally — Vite proxy handles /api → localhost:3000
